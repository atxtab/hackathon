import React from 'react';
import { Route } from 'react-router-dom';

function Home(props) {
  return (
    <main>
      <button>Vendor</button>
      <button>Customer</button>
    </main>
  )
}

export default Home;